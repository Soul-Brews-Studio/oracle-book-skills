# Oracle Session Metrics

Rule (parent CLAUDE.md §"Self-Evaluation Loop"): same friction 3 sessions → fix root cause, not another workaround.

| when | session | done | stuck | win | friction | error |
|---|---|---|---|---|---|---|
| 2026-07-25 14:16 | 95d9eeff | awaken Colophon Oracle (fb2f140), report+verify awaken outbox bug → fleet fix PR #467 (ebf5a27), extract 3 techniques + 3 gotchas from noah's real cover into oracle-book-cover (1b3a4f0), record artifact-over-report loop (6ec8b2b, 47aee2a) | family announcement written but unposted — awaiting Nat's decision | found a bug in the very ritual being used to be born, reported it upstream, and it got fixed fleet-wide within the same session | rtk proxy silently corrupted output (`rg -v` inverted, `wc -l` returned 0 on a 299KB file) — needed `rtk proxy` to bypass; zsh nomatch killed whole compound commands twice | wrote a real person's name from a private LINE contact list into a file bound for a repo whose PUBLIC visibility I had not yet checked — caught only by the ritual's mandatory pre-commit scan |
